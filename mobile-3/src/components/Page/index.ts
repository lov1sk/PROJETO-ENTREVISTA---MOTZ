import { PageHeader } from "./Header";
import { PageRoot } from "./Root";
import { PageSection } from "./Section";

export const Page = {
  Root: PageRoot,
  Header: PageHeader,
  Section: PageSection,
};
